<%= form_for(@background_two) do |f| %>
  <% if @background_two.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@background_two.errors.count, "error") %> prohibited this background_two from being saved:</h2>

      <ul>
      <% @background_two.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_area :image %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
